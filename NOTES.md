# Tinkering Notes

## 2026-08-14

- Tried out a quick SQLite pivot using `GROUP BY` + `MAX(CASE ...)` — works but feels clunky.
- Might explore `FILTER` syntax next time.

## Next ideas

- Look into DuckDB for in-memory aggregations.
- Clean up old experiments in `scratch/`.
